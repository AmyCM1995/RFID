{% extends 'base.html.twig' %}

{% block title %}BorradoPropio index{% endblock %}

{% block body %}
    <h1>BorradoPropio index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Codigo</th>
                <th>Detalle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for borrado_propio in borrado_propios %}
            <tr>
                <td>{{ borrado_propio.id }}</td>
                <td>{{ borrado_propio.codigo }}</td>
                <td>{{ borrado_propio.detalle }}</td>
                <td>
                    <a href="{{ path('borrado_propio_show', {'id': borrado_propio.id}) }}">show</a>
                    {% if borrado_propio.esEditable == true %}
                         <a href="{{ path('borrado_propio_edit', {'id': borrado_propio.id}) }}">edit</a>
                    {% endif %}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('borrado_propio_new') }}">Create new</a>
{% endblock %}
