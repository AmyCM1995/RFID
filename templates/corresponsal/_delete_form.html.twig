<form method="post" action="{{ path('corresponsal_delete', {'id': corresponsal.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ corresponsal.id) }}">
    <button class="btn">Delete</button>
</form>
