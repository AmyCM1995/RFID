{{ form_start(form) }}
{{ form_widget(form) }}
<br>
<button class="btn btn-primary">
    {{ button_label|default('Aceptar') }}
</button>
<a class="btn btn-primary" href="{{ path('lector_index') }}">Regresar</a>
{{ form_end(form) }}
