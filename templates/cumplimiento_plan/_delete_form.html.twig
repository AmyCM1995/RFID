<form method="post" action="{{ path('cumplimiento_plan_delete', {'id': cumplimiento_plan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cumplimiento_plan.id) }}">
    <button class="btn">Delete</button>
</form>
