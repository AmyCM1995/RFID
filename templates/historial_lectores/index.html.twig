{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block contenido %}
    {{ parent() }}
    <h1>Historial del lector {{ lector.nombre }}</h1>
    <p>Código: {{ lector.codigo }}</p>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Resultado</th>
                <th>Estado</th>
            </tr>
        </thead>
        <tbody>
        {% for historial_lectore in historial_lectores %}
            <tr>
                <td>{{ historial_lectore.id }}</td>
                <td>{{ historial_lectore.resultado }}</td>
                <td>{{ historial_lectore.estado }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">No existe historial para este lector</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
