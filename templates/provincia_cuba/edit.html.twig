{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block contenido %}
    {{ parent() }}

    <h1>Editar provincias de Cuba</h1>

    {{ include('provincia_cuba/_form.html.twig', {'button_label': 'Actualizar'}) }}


    <div class="row ">
        <ul style="margin-left: 15px; padding: 0">
            <div style="margin-top: 2px">{{ include('provincia_cuba/_delete_form.html.twig') }}</div>

            <button class="btn bg-primary" id="corresponsal-btns" style="margin-top: 3px; padding-left: 0">
                <a href="{{ path('provincia_cuba_index') }}" style="margin-left: 10px">Regresar</a>
            </button>
        </ul>
    </div>
{% endblock %}
