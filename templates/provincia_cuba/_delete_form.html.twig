<form method="post" action="{{ path('provincia_cuba_delete', {'id': provincia_cuba.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ provincia_cuba.id) }}">
    <button class="btn">Delete</button>
</form>
