<form method="post" action="{{ path('pais_correspondencia_delete', {'id': pais_correspondencium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pais_correspondencium.id) }}">
    <button class="btn bg-primary">
        <a style="color: #FFFFFF">Borrar</a>
    </button>
</form>
