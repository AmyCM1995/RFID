    {{ form_start(form) }}
    {{ form_row(form.fecha) }}
    <br>
    <input type="checkbox" name="rango" value="off" onclick="mostrarFechaFin()"> Rango de fechas
    <br>
    <div id="fechaFin" style="display: none">
        <br>
        Fecha fin <input type="date" name="fechaFin" class="form-control" id="fechaFin">
    </div>
    <br>
    {{ form_row(form.es_anual) }}
    {{ form_row(form.descripcion) }}
    {{ form_row(form.provincia) }}
<br>
<button class="btn btn-primary">
    {{ button_label|default('Aceptar') }}
</button>
<a class="btn btn-primary" href="{{ path('fechas_no_correspondencia_index') }}">Regresar</a>

    <script type="text/javascript">
        function mostrarFechaFin() {
            document.getElementById("fechaFin").style.display = "block";
        }
    </script>

{{ form_end(form) }}
