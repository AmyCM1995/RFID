{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}

{% block contenido %}
    {{ parent() }}
    <h1>Fechas no correspondencia</h1>

    {{ include('fechas_no_correspondencia/_form.html.twig') }}

    <button class="btn bg-primary" id="corresponsal-btns" style="margin-top: 2px">
        <a href="{{ path('fechas_no_correspondencia_index') }}">Regresar</a>
    </button>

{% endblock %}
