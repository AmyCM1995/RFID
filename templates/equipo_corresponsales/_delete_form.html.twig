<form method="post" action="{{ path('equipo_corresponsales_delete', {'id': equipo_corresponsale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipo_corresponsale.id) }}">
    <button class="btn">Delete</button>
</form>
