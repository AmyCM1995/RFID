
<form method="post" action="{{ path('plan_imposicion_c_s_v_delete', {'id':11}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ 11) }}">
    <button class="btn">Delete</button>
</form>
