{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}{% endblock %}
{% block jquery%}{{ parent() }}{% endblock %}
{%  block head %}
<script type="text/javascript">
    $(document).ready(function () {
        $('.button').on('click', function () {
            //Añadir el spinner
            $('#content').html('<br> <div class="d-flex justify-content-center"><div class="spinner-border text-primary" role="status"><span class="sr-only"> Importando...</span>\</div></div>');
            $.ajax({
                type: "POST",
                url: {{ path('plan_imposicion_persistir') }},
                success: function (data) {
                    $('#content').fadeIn(1000).html(data);
                }
            });
            return false;
        });
    });
</script>
{% endblock %}
{% block contenido %}
       {{ parent() }}
    <h3>Importar nuevo plan de imposición</h3>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}


    {#<div class="container">
        <div class="row">
            <div class="col-md-12">
                <p>&nbsp;</p>
                <p>&nbsp;</p>
                <button type="button" id="buton1" class="btn btn-success btn-block">Iniciar</button>
                <p>&nbsp;</p>
                <button type="button" id="buton2" class="btn btn-success btn-block">Parar</button>
            </div>

            <div class="col-md-12">
                <p>&nbsp;</p>
                <p>&nbsp;</p>
                <div class="progress-bar-striped progress-bar-animated" id="progressbar" style="border:1px solid #ccc; border-radius: 5px; "></div>

                <!-- Progress information -->
                <br>
                <div id="information" ></div>
            </div>
        </div>
    </div>

    <iframe id="loadarea" style="display: none;"></iframe><br />
    #}


{% endblock %}
