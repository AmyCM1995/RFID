{% extends 'base.html.twig' %}

{% block title %}PlanDeImposicion{% endblock %}

{% block body %}
    <h1>PlanDeImposicion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ plan_de_imposicion.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ plan_de_imposicion.fecha ? plan_de_imposicion.fecha|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Cod_corresponsal_destino</th>
                <td>{{ plan_de_imposicion.codCorresponsalDestino }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('plan_de_imposicion_index') }}">back to list</a>

    <a href="{{ path('plan_de_imposicion_edit', {'id': plan_de_imposicion.id}) }}">edit</a>

    {{ include('plan_de_imposicion/_delete_form.html.twig') }}
{% endblock %}
